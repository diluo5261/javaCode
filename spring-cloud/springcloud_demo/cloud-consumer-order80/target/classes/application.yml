server:
  port: 80

spring:
  application:
    name: cloud-consumer-order80
  devtools:
    livereload:
      port: 35730

eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:7001/eureka
