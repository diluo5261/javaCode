<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

<!--    启动spring的注解支持-->
    <context:annotation-config/>

<!--    配置一个用于专门配置SpringSession的bean标签配置
        只配置 RedisHttpSessionConfiguration 的bean 就可以实现同域名同项目的session共享
        适合于我们的 Nginx 集群模式下的 P2P 项目部署
-->
    <bean class="org.springframework.session.data.redis.config.annotation.web.http.RedisHttpSessionConfiguration">

<!--        配置session的最大声明周期,单位是秒,默认是 1800秒,30分钟-->
        <property name="maxInactiveIntervalInSeconds" value="1800"/>
<!--        注入一个 Cookie的序列化规则对象-->
        <property name="cookieSerializer" ref="defaultCookieSerializer"/>
    </bean>

<!--    配置一个cookie序列化规则对象,用于改变 cookie的存放规则-->
    <bean id="defaultCookieSerializer" class="org.springframework.session.web.http.DefaultCookieSerializer">
<!--        指定springSession的sessionid 存放在域名的根路径下,用于实现同域名不同项目的Session共享-->
        <property name="cookiePath" value="/"/>

<!--        指定springsession 的 SessionId存放我在根域名下,用于实现同根域名不同二级子域名下的session共享
            适合应用在 Nginx 的虚拟主机的多城市站点部署
-->
        <property name="domainName" value="myweb.coom"/>
    </bean>

    <!-- 配置jedis连接工厂，用于连接redis -->
    <bean id="jedisConnectionFactory" class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory">
        <property name="hostName" value="192.168.169.128"/>
        <property name="port" value="6379"/>


    </bean>

</beans>